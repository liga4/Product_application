<!DOCTYPE html>
<html lang="en" class="h-full bg-gray-100">
<head>
    <meta charset="UTF-8">
    <title>ScandiWeb Test assignment</title>
    <script src="https://cdn.tailwindcss.com?plugins=forms"></script>

</head>
<body class="h-full">

{% if flash_messages is defined %}
    {% for flashMessage in flash_messages %}
        <div class="error-summary">
                <p style="color: red;">{{ flashMessage.message }}</p>
        </div>
    {% endfor %}
{% endif %}
